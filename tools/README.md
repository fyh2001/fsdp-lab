# Tools

跨 issue / 跨模型复用的小工具。

候选内容（按需添加）：

- **profiler 包装器**：torch.profiler / nsys / cProfile 的简化入口
- **checkpoint 检查**：FSDP sharded ckpt 的完整性 / 形状 / 数值校验
- **log 解析**：从训练日志提取 loss/grad/throughput 时序，画对比图
- **OOM 诊断**：解析 `torch.cuda.memory._snapshot()` dump
- **通信抓包**：NCCL log 解析 + 时间线可视化

## 索引

| 工具 | 用途 | 文档 |
|---|---|---|
| _暂无_ | | |
